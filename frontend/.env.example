# Leave empty to use Vite dev proxy (/api, /health -> backend). Or set e.g. http://localhost:8000
VITE_API_BASE_URL=
VITE_DEMO_USER_ID=demo-user
# faculty | student | alumni | all
VITE_DEMO_USER_ROLE=all
VITE_DEMO_BEARER_TOKEN=
